import React from 'react';

export default function Dashboard(){
  return (
    <div>
      <h2>Dashboard</h2>
      <p>Use the API wrapper in `src/api.js` to call backend endpoints.</p>
    </div>
  );
}
import React from 'react';

export default function Dashboard(){
  return (
    <div>
      <h2>Dashboard</h2>
      <p>Usage summary and outstanding invoices will be shown here.</p>
    </div>
  )
}
